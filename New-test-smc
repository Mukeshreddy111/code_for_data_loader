ed = dict_name[col][key]  # this is the EditorSettings dict

hardcoded_items = ["Argentina", "Australia", "Austria", "Belgium"]
hardcoded_selected = ["Australia", "Belgium"]

# write to every plausible key name (covers py39–py311 differences)
for k in ["Items", "items", "Options", "options", "List", "list", attr]:
    ed[k] = hardcoded_items

for k in ["value", "Value", "selected"]:
    ed[k] = hardcoded_selected

print(">>> JUR keys:", list(ed.keys()))
print(">>> JUR Items:", ed.get("Items"))
print(">>> JUR Options:", ed.get("Options"))
print(">>> JUR List:", ed.get("List"))
print(">>> JUR Selected:", ed.get("value") or ed.get("selected"))
# --- END TEST BLOCK ---
That’s cleaner (less repetition) but identical to your pasted lines.
2️⃣ In onCubeInitialized()
Right before the line that calls
self.sync_regulation_ui_to_model(...)
add this small live check:
props = self.AtomComplexGrid().ColumnProperties()
ed_live = props.get("Jurisdiction", {}).get("EditorSettings", {})
print(">>> LIVE JUR keys:", list(ed_live.keys()))
print(">>> LIVE lens:", 
      len(ed_live.get("Items") or []),
      len(ed_live.get("Options") or []),
      len(ed_live.get("List") or []))
print(">>> LIVE selected:", ed_live.get("value") or ed_live.get("selected"))
